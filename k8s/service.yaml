apiVersion: v1
kind: Service
metadata:
  name: acs-income-predictor-service
  labels:
    app: acs-income-predictor
spec:
  type: NodePort
  selector:
    app: acs-income-predictor
  ports:
    - name: http
      port: 80
      targetPort: 5000
      nodePort: 30007
